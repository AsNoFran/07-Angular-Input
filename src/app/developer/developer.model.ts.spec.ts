import { DeveloperModelTs } from '../model/developer.model';

describe('DeveloperModelTs', () => {
  it('should create an instance', () => {
    expect(new DeveloperModelTs()).toBeTruthy();
  });
});
